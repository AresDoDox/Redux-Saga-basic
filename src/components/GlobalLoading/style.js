const styles = () => ({
  BoxLoading: {
    position: 'fixed',
    top: 0,
    left: 0,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100vw',
    height: '100vh',
    background: 'rgba(0,0,0,0.5)',
    zIndex: 100
  },
  imgLoading: {
    width: 100
  }
})

export default styles
